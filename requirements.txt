# ── Text-to-Image GAN ── requirements.txt ────────────────────────────────────
# Python 3.9 – 3.11 recommended
#
# ── Core Deep Learning ────────────────────────────────────────────────────────
torch>=2.1.0               # M2 MPS support (2.x) / CUDA (Colab T4)
torchvision>=0.16.0

# ── OpenAI CLIP (text encoder) ───────────────────────────────────────────────
# Install directly from GitHub (no PyPI release):
#   pip install git+https://github.com/openai/CLIP.git
ftfy>=6.1.1                # CLIP dependency — text normalisation
regex>=2023.6.3            # CLIP dependency

# ── Data & Utilities ─────────────────────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
Pillow>=10.0.0
tqdm>=4.65.0
scikit-learn>=1.3.0        # optional: metrics / eval utilities

# ── Visualisation ─────────────────────────────────────────────────────────────
matplotlib>=3.7.0

# ── Dataset Download ─────────────────────────────────────────────────────────
kaggle>=1.5.16             # Flickr8k via Kaggle API (optional)

# ── Notebook Runtime ─────────────────────────────────────────────────────────
jupyterlab>=4.0.0          # or: notebook>=7.0.0
ipywidgets>=8.0.0          # progress bars inside notebooks

# ── INSTALLATION ORDER ───────────────────────────────────────────────────────
# 1. Install PyTorch first (use the official selector for your platform):
#    https://pytorch.org/get-started/locally/
#
#    Apple M2 (MPS):
#      pip install torch torchvision
#
#    Google Colab / CUDA T4:
#      pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#
# 2. Install CLIP:
#      pip install git+https://github.com/openai/CLIP.git
#
# 3. Install remaining deps:
#      pip install -r requirements.txt
